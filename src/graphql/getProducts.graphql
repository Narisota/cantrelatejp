query getProducts {
    getProducts {
        product_id
        name
        desc
        price
        stock
        org_stock
        exp_date
        images {
            img_id
            img_url
        }
    }
}
