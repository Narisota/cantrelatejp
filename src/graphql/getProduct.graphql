query getProduct($product_id: Float!) {
    getProduct(product_id: $product_id) {
        product_id
        name
        desc
        price
        stock
        org_stock
        exp_date
        images {
            img_id
            img_url
        }
    }
}
